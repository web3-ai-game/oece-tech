# 🏨 Hotel Inistel - 外置SSD优化配置说明

## ✅ 环境已就绪

您的酒店管理系统已配置为**平衡性能开发环境**，充分利用外置SSD的高速读写能力。

### 🌐 服务访问地址
- **React前端**: http://localhost:3000
- **后端API**: http://localhost:5001/api/health
- **PostgreSQL**: localhost:5433 (用户: postgres, 密码: password)
- **Redis缓存**: localhost:6379

### 💽 外置SSD存储映射
```
/Volumes/128/hotel-docker/
├── postgres-data/     # PostgreSQL数据库文件
├── sqlite-data/       # SQLite数据库 (hotel.db)
├── redis-data/        # Redis持久化数据
└── frontend-cache/    # React构建缓存
```

### ⚡ 性能优势
1. **500MB/s** SSD读写速度
2. **减轻内存压力** - 数据存储外置
3. **保留系统资源** - 仅使用1.9GB内存
4. **CPU合理分配** - 总计3.5核心

### 🚀 日常使用命令

```bash
# 启动开发环境
./start.sh start

# 查看服务状态
./start.sh stats

# 查看SSD使用情况
./start.sh ssd

# 查看实时日志
./start.sh logs

# 停止服务
./start.sh stop

# 重启服务
./start.sh restart
```

### 📊 资源监控

#### 查看容器资源使用
```bash
./start.sh stats
```

#### 查看外置SSD状态
```bash
./start.sh ssd
```

#### 实时监控所有服务
```bash
docker-compose logs -f
```

### 🔧 开发工作流

1. **修改代码**: 直接编辑项目文件
2. **热重载**: React和Node.js都支持热重载
3. **数据库操作**: 
   - PostgreSQL: 使用任何客户端连接 localhost:5433
   - SQLite: 直接访问 `/Volumes/128/hotel-docker/sqlite-data/hotel.db`
4. **缓存管理**: Redis数据持久化到外置SSD

### 📈 性能优化特性

- **内存优化**: 容器内存限制防止资源溢出
- **CPU调度**: 合理分配CPU资源给各服务
- **存储优化**: 高速SSD存储提升数据访问速度
- **网络优化**: Docker网络隔离提升安全性
- **缓存优化**: 前端构建缓存存储到SSD

### 🛠️ 故障排除

**端口被占用？**
```bash
./start.sh stop
./start.sh start
```

**服务启动失败？**
```bash
./start.sh logs
```

**SSD空间不足？**
```bash
./start.sh ssd
# 检查数据使用情况
```

**完全重置环境？**
```bash
./start.sh clean
# 会提示确认删除所有数据
```

### 📝 项目结构
```
hotel-inistel/
├── backend/              # 后端API服务
│   ├── server.js        # Express服务器
│   ├── database.js      # SQLite数据库配置
│   └── Dockerfile.light # 轻量化Docker配置
├── frontend/            # React前端
├── database/            # PostgreSQL初始化
├── docker-compose.yml   # Docker编排配置
├── start.sh            # 一键管理脚本
└── data/               # 本地数据目录
```

### 🎯 下一步建议

1. **API开发**: 在 `backend/server.js` 中扩展API接口
2. **前端功能**: 开发React组件和页面
3. **数据库设计**: 扩展SQLite表结构
4. **性能监控**: 定期检查 `./start.sh stats`
5. **备份策略**: 定期备份 `/Volumes/128/hotel-docker/`

---
**🚀 您的高性能酒店管理系统开发环境已就绪！**

总内存占用: ~1.9GB | 外置SSD存储 | 500MB/s读写速度
