# 🔐 AI Key 調用規則 - SMS-Key 項目

> **嚴格保密**: 所有收費 key 必須加密存儲，僅通過 Doppler 訪問
> 
> **最後更新**: 2025-11-26

---

## 📋 Key 清單與用途

### 🆓 免費層 Key（優先使用）

```bash
# Gemini 免費層 - 項目開發測試用
GEMINI_FREE_1="AIzaSyD_cNll0AKAmKZgO6pOJzMRosKiBJxuUNM"
  用途: 代碼生成、文檔生成、快速測試
  限制: 每分鐘 60 請求
  優先級: ⭐⭐⭐⭐⭐ (最優先)
```

### 💰 收費層 Key（嚴格保密，按順序使用）

```bash
# Doppler 收費 Key #1（主力）
DOPPLER_PROD_1="AQ.Ab8RN6LlrNEKtXonwqhBKhVRziaoBgHiUwE6CpdSv5Ttil4JgA"
  用途: 生產環境密鑰管理
  優先級: ⭐⭐⭐⭐

# Doppler 收費 Key #2（備用）
DOPPLER_PROD_2="AQ.Ab8RN6LioS7k0Ipycl6oKXFuhww6VTXuosXwgeS8VMpTyZUFcw"
  用途: 備用密鑰管理、故障轉移
  優先級: ⭐⭐⭐⭐

# Gemini 收費 Key #3（2.5 Pro）
GEMINI_PRO_25="AIzaSyAj08QZ4B8CMU_CTG-QtGUEv0gBHZbM_cQ"
  用途: 高難度任務、架構設計
  模型: Gemini 2.5 Pro
  優先級: ⭐⭐⭐

# Gemini 收費 Key #4（3.0 Pro）
GEMINI_PRO_30="AIzaSyA3ikY04T94AoAwndr20QxV9nl4w_NF_u4"
  用途: 超高難度任務、最終審核
  模型: Gemini 3.0 Pro
  優先級: ⭐⭐ (最後使用)
```

### 🌐 OpenRouter / XAI（低成本補充）

```bash
# XAI - 極低成本代碼生成
XAI_KEY="使用最便宜的模型，甚至免費級別"
  用途: 大量代碼生成、快速迭代
  優先級: ⭐⭐⭐⭐
```

### 🚫 不使用的 Key

```bash
# Claude Code / Claude Pro
# ❌ 用戶有 Windsurf 和 Claude Pro 訂閱
# ❌ 會小心使用自己的 Claude Code 額度
# ❌ 無需調用集成 key
```

---

## 🎯 調用規則（嚴格執行）

### 規則 1: 優先級順序

```
1️⃣ GEMINI_FREE_1 (AIzaSyD_cNll0AKAmKZgO6pOJzMRosKiBJxuUNM)
   ↓ 如果超限或不適用
2️⃣ XAI (最便宜模型)
   ↓ 如果需要高質量
3️⃣ GEMINI_PRO_25 (AIzaSyAj08QZ4B8CMU_CTG-QtGUEv0gBHZbM_cQ)
   ↓ 如果超高難度
4️⃣ GEMINI_PRO_30 (AIzaSyA3ikY04T94AoAwndr20QxV9nl4w_NF_u4)
   ↓ 絕不使用（除非緊急）
5️⃣ Claude Code / Claude Pro
```

### 規則 2: 任務分配

| 任務類型 | 使用 Key | 原因 |
|---------|---------|------|
| 代碼生成（常規） | GEMINI_FREE_1 / XAI | 免費 + 便宜 |
| 文檔生成 | GEMINI_FREE_1 | 免費夠用 |
| 快速測試 | GEMINI_FREE_1 | 免費夠用 |
| 架構設計 | GEMINI_PRO_25 | 需要高質量 |
| 複雜調試 | GEMINI_PRO_25 | 需要推理能力 |
| 最終審核 | GEMINI_PRO_30 | 超高難度 |
| 大量代碼噴發 | XAI (免費模型) | 成本最低 |

### 規則 3: 溫度設置

```javascript
const TEMPERATURE_RULES = {
  // 低溫精確（0.2-0.3）
  extraction: {
    model: 'GEMINI_FREE_1',
    temperature: 0.3,
    use: '提取核心內容、去重、數據清洗'
  },
  
  // 高溫創意（0.8-0.9）
  expansion: {
    model: 'GEMINI_PRO_25',
    temperature: 0.9,
    use: '創意擴展、實戰案例、架構圖'
  },
  
  // 精準修正（0.1-0.2）
  correction: {
    model: 'GEMINI_PRO_30',  // 僅在必要時
    temperature: 0.2,
    use: '最終修正、去除飄移、技術審核'
  }
};
```

### 規則 4: 成本控制

```bash
# 每日預算（USD）
FREE_TIER: $0 (無限制，但有 RPM 限制)
XAI: $1 (低成本大量使用)
GEMINI_PRO_25: $5 (高質量任務)
GEMINI_PRO_30: $10 (緊急情況)

# 監控命令
watch -n 60 'doppler secrets get GEMINI_USAGE --project sms --config prod'
```

---

## 🔧 Doppler 配置

### 更新收費 Key

```bash
cd ~/sms-key

# 更新免費 Key
doppler secrets set GEMINI_FREE_1="AIzaSyD_cNll0AKAmKZgO6pOJzMRosKiBJxuUNM" \
  --project sms --config prod

# 更新收費 Key #1
doppler secrets set DOPPLER_PROD_1="AQ.Ab8RN6LlrNEKtXonwqhBKhVRziaoBgHiUwE6CpdSv5Ttil4JgA" \
  --project sms --config prod

# 更新收費 Key #2
doppler secrets set DOPPLER_PROD_2="AQ.Ab8RN6LioS7k0Ipycl6oKXFuhww6VTXuosXwgeS8VMpTyZUFcw" \
  --project sms --config prod

# 更新 Gemini 2.5 Pro
doppler secrets set GEMINI_PRO_25="AIzaSyAj08QZ4B8CMU_CTG-QtGUEv0gBHZbM_cQ" \
  --project sms --config prod

# 更新 Gemini 3.0 Pro
doppler secrets set GEMINI_PRO_30="AIzaSyA3ikY04T94AoAwndr20QxV9nl4w_NF_u4" \
  --project sms --config prod

# 拉取最新配置
doppler secrets download --no-file --format env > .env.doppler
```

### 驗證配置

```bash
# 驗證所有 Key
bash ~/verify_all_keys.sh

# 檢查特定 Key
doppler secrets get GEMINI_FREE_1 --project sms --config prod --plain
```

---

## 🧪 測試腳本

### 測試免費 Key

```bash
#!/bin/bash
# test_gemini_free.sh

GEMINI_FREE_1="AIzaSyD_cNll0AKAmKZgO6pOJzMRosKiBJxuUNM"

curl -X POST \
  "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-exp:generateContent?key=$GEMINI_FREE_1" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{
      "parts": [{
        "text": "測試連接，返回 OK"
      }]
    }],
    "generationConfig": {
      "temperature": 0.3
    }
  }'
```

### 測試收費 Key

```bash
#!/bin/bash
# test_gemini_pro.sh

# 測試 2.5 Pro
GEMINI_PRO_25="AIzaSyAj08QZ4B8CMU_CTG-QtGUEv0gBHZbM_cQ"

curl -X POST \
  "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-pro:generateContent?key=$GEMINI_PRO_25" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{
      "parts": [{
        "text": "高難度測試：設計一個分佈式系統架構"
      }]
    }],
    "generationConfig": {
      "temperature": 0.7
    }
  }'
```

---

## 📊 使用統計（每週更新）

```markdown
| 週期 | 免費使用量 | XAI 成本 | Pro 2.5 成本 | Pro 3.0 成本 | 總成本 |
|------|-----------|---------|-------------|-------------|--------|
| W47  | 無限      | $0.50   | $3.20       | $0.00       | $3.70  |
| W48  | -         | -       | -           | -           | -      |
```

---

## ⚠️ 安全警告

### 🚨 絕對禁止

- ❌ 將收費 key 寫入代碼
- ❌ 提交 key 到 Git
- ❌ 在公開渠道分享
- ❌ 使用未加密的配置文件

### ✅ 安全實踐

- ✅ 所有 key 通過 Doppler 管理
- ✅ 本地使用環境變量
- ✅ 定期輪換 key
- ✅ 監控異常使用

---

## 🔄 Key 輪換計劃

```bash
# 每月輪換（第一個星期一）
# 1. 生成新 Key
# 2. 更新 Doppler
# 3. 測試新 Key
# 4. 撤銷舊 Key
```

---

**維護者**: DeepWeay SMS Team  
**最後更新**: 2025-11-26  
**版本**: v1.0  
**⚠️ 機密等級**: 🔴 **CONFIDENTIAL**

