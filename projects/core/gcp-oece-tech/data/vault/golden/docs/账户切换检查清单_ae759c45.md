# Windsurf账户切换检查清单

> **目的**: 确保从当前账户无缝切换到新账户继续开发  
> **项目**: Urban Diver (DeepWeay)  
> **更新**: 2025-11-06

---

## ✅ 切换前准备 (当前账户)

### 1. 代码同步
- [ ] 所有本地改动已commit
- [ ] Push到GitHub main分支
- [ ] 确认GitHub上代码最新
- [ ] 检查是否有未完成的分支

```bash
cd /svs/studio  # 或 /mnt/external-ssd/svs/studio
git status
git add -A
git commit -m "chore: 账户切换前最终备份"
git push origin main
```

### 2. 数据库备份
- [ ] 导出Supabase schema
- [ ] 导出测试数据 (users, bbs_posts, bbs_replies)
- [ ] 保存SQL文件到项目配置目录

```bash
# 在Supabase Dashboard SQL Editor执行
-- 导出所有数据
SELECT * FROM users;
SELECT * FROM invites;
SELECT * FROM bbs_posts;
SELECT * FROM bbs_replies;
```

### 3. 环境变量记录
- [ ] 复制`.env.local`所有内容
- [ ] 记录Supabase URL和Keys
- [ ] 记录Gemini API Key
- [ ] 保存到密码管理器

### 4. Token收集
- [ ] GitHub Personal Access Token (有效期检查)
- [ ] Supabase Access Token
- [ ] DigitalOcean API Token (可选)
- [ ] 所有Token保存到安全位置

### 5. 配置文件确认
- [ ] `项目配置/vps.md` 已创建
- [ ] `项目配置/windsurf-rules-backup.md` 已创建
- [ ] `项目配置/windsurf-global-rules.md` 已创建
- [ ] `项目配置/mcp-settings.json` 已创建
- [ ] `项目配置/账户切换检查清单.md` 已创建

### 6. 开发进度记录
- [ ] 记录当前开发到哪个功能
- [ ] 列出待完成任务清单
- [ ] 标注已知Bug和TODO
- [ ] 更新README.md状态

当前状态 (2025-11-06):
```
✅ 已完成:
- 用户认证系统 (Supabase Auth + 邀请码)
- BBS论坛 (完整UI/UX + 管理员功能)
- Dashboard (用户统计 + BBS活动统计)
- 响应式布局
- 测试数据导入

⏳ 待完成:
- AI工具页面 (Gemini集成)
- 文章系统 (Markdown编辑器)
- PRO会员订阅 (Stripe)
- Telegram Bot
- 静态页面分离部署
```

---

## 🔄 新账户设置 (下一个账户)

### 1. 安装Windsurf
- [ ] 下载最新版: https://www.codeium.com/windsurf
- [ ] 使用新邮箱注册
- [ ] 激活账户
- [ ] 检查点数余额

### 2. 克隆项目
- [ ] 本地开发: `git clone git@github.com:web3-ai-game/studio.git`
- [ ] VPS开发: `cd /mnt/external-ssd/svs/studio && git pull`
- [ ] 在Windsurf中打开项目
- [ ] 确认项目结构完整

### 3. 导入全局规则
- [ ] 打开 `Settings` → `Memories` → `Global Rules`
- [ ] 复制 `项目配置/windsurf-global-rules.md` 全部内容
- [ ] 粘贴到Global Rules文本框
- [ ] 保存

验证:
```
在Cascade中问: "项目的主要技术栈是什么？"
预期答案: Next.js 15.5.6 + Supabase + React 19
```

### 4. 导入项目规则
- [ ] 打开 `Settings` → `Memories` → `Project Rules`
- [ ] 复制 `项目配置/windsurf-rules-backup.md` 全部内容
- [ ] 粘贴到Project Rules文本框
- [ ] 保存

验证:
```
在Cascade中问: "当前项目名称和域名是什么？"
预期答案: Urban Diver, deepweay.me
```

### 5. 配置MCP工具

#### 5.1 Filesystem
```json
{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-filesystem", "/svs/studio"]
}
```

#### 5.2 GitHub (需要新token)
- [ ] 生成GitHub PAT: https://github.com/settings/tokens/new
- [ ] 权限: `repo`, `workflow`, `write:packages`
- [ ] 复制token

```json
{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-github"],
  "env": {
    "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_你的新token"
  }
}
```

#### 5.3 Supabase (使用相同项目)
- [ ] 登录Supabase: https://supabase.com
- [ ] 项目: qhgdymgxcbyhtxezvoqt
- [ ] Project Settings → API → Access Tokens → 创建新token
- [ ] 复制token

```json
{
  "command": "npx",
  "args": ["-y", "@shinework/supabase-mcp-server"],
  "env": {
    "SUPABASE_ACCESS_TOKEN": "你的token"
  }
}
```

#### 5.4 Memory & Sequential-Thinking
```json
{
  "memory": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"]
  },
  "sequential-thinking": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
  }
}
```

### 6. 恢复环境变量
- [ ] 创建 `.env.local`
- [ ] 复制之前保存的所有环境变量
- [ ] 确认Supabase连接
- [ ] 测试Gemini API可用

```bash
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://qhgdymgxcbyhtxezvoqt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
GOOGLE_GENAI_API_KEY=AIza...
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 7. 验证MCP工具
- [ ] 重启Windsurf
- [ ] 在Cascade中测试

```
测试命令:
1. "列出可用的MCP工具"
   预期: filesystem, github, supabase, memory, sequential-thinking

2. "使用filesystem读取package.json的name字段"
   预期: "studio"

3. "使用github列出最近3次commit"
   预期: 显示提交历史

4. "使用supabase查询users表总数"
   预期: 返回用户数量
```

### 8. 安装依赖并测试
```bash
cd /svs/studio
npm install
npm run dev
```

- [ ] Dev server启动成功 (http://localhost:3000)
- [ ] 访问首页正常
- [ ] 登录功能正常
- [ ] Dashboard显示正常
- [ ] BBS论坛可访问

---

## 🚨 常见问题

### Q1: MCP工具无法连接
**解决**:
1. 检查网络连接
2. 确认token有效期
3. 重启Windsurf
4. 查看Console错误日志

### Q2: Supabase连接失败
**解决**:
1. 检查`.env.local`环境变量
2. 确认Supabase项目在线
3. 验证Access Token权限
4. 测试: `node test-supabase.js`

### Q3: GitHub push失败
**解决**:
1. 检查SSH key或PAT
2. 确认仓库权限
3. 测试: `git remote -v`
4. 重新配置: `git remote set-url origin git@github.com:web3-ai-game/studio.git`

### Q4: 前任账户的记忆无法导入
**解决**:
- Windsurf记忆是账户级别的，无法直接迁移
- 但项目规则和全局规则可以手动复制
- 重要上下文已保存在 `项目配置/` 目录

### Q5: VPS外挂盘访问权限问题
**解决**:
```bash
# SSH到VPS
ssh root@134.209.142.24

# 检查挂载
df -h | grep external

# 修复权限
sudo chown -R $USER:$USER /mnt/external-ssd

# 重新挂载
sudo mount /dev/sda /mnt/external-ssd
```

---

## 📊 切换完成验证

### 最终测试清单
- [ ] 创建一个测试commit并push成功
- [ ] 在Dashboard查看BBS统计
- [ ] 发布一条测试帖子
- [ ] 回复一条帖子
- [ ] 生成一个邀请码
- [ ] 退出登录再登录

### 性能检查
- [ ] 页面加载速度 < 3秒
- [ ] 无Console错误
- [ ] 移动端响应正常
- [ ] 所有链接可点击

### 开发工作流测试
```bash
# 测试完整开发流程
1. 修改一个文件
2. Git commit
3. 在Cascade中说: "帮我push到GitHub"
4. 确认GitHub上有新commit
```

---

## 🎯 继续开发建议

### 优先级排序
1. **高优先级**: AI工具页面 (核心卖点)
2. **中优先级**: 文章系统、PRO订阅
3. **低优先级**: Telegram Bot、数据分析

### 开发顺序
```
Week 1: AI工具页面基础框架
Week 2: Gemini集成 + 测试
Week 3: 文章系统 (Markdown编辑器)
Week 4: Stripe订阅集成
Week 5: 静态页面分离部署
Week 6: 性能优化 + 上线准备
```

### 注意事项
- 每天结束前push代码
- 重要功能完成后立即备份
- 定期测试VPS外挂盘可用性
- 保持项目配置目录更新

---

## 📞 紧急联系方式

- **GitHub**: https://github.com/web3-ai-game/studio
- **Supabase**: qhgdymgxcbyhtxezvoqt
- **VPS**: root@134.209.142.24
- **域名**: deepweay.me

---

**检查清单创建**: 2025-11-06  
**下次更新**: 账户切换完成后或重大变更时
