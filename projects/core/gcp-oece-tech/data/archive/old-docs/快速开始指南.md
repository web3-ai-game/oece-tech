# 快速开始指南 | oece.tech × deepweay.me

> 两个网站系统的实战开发和部署指南

---

## 🎯 系统快速对比

| 特性 | oece.tech | deepweay.me |
|------|-----------|------------|
| **定位** | 极客生态导航 | 赛博算命 AI 聊天 |
| **核心功能** | 项目导航+知识库 | AI 对话+内容生成 |
| **主框架** | Next.js 16 | Next.js 16 |
| **主数据库** | MongoDB | Firebase + MongoDB |
| **AI 能力** | OpenRouter | Gemini Pro |
| **部署平台** | Firebase Hosting | Firebase Hosting + GCP |
| **主要成本** | 搜索/监控 | AI API 调用 |

---

## 🚀 项目 1: oece.tech

### 项目结构
```
oece-tech/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx
│   │   ├── page.tsx            # 首页
│   │   ├── projects/
│   │   │   └── [id]/
│   │   │       └── page.tsx    # 项目详情
│   │   ├── search/
│   │   │   └── page.tsx        # 搜索结果
│   │   ├── tools/
│   │   │   └── page.tsx        # AI 工具展示
│   │   ├── docs/
│   │   │   ├── page.tsx        # 文档首页
│   │   │   └── [...slug]/
│   │   │       └── page.tsx    # 文档详情
│   │   └── api/
│   │       ├── projects/
│   │       ├── search/
│   │       ├── tools/
│   │       └── webhook/
│   │           └── telegram.ts
│   ├── components/
│   │   ├── ProjectCard.tsx
│   │   ├── ProjectGrid.tsx
│   │   ├── SearchBar.tsx
│   │   ├── ToolCard.tsx
│   │   ├── Navigation.tsx
│   │   └── Footer.tsx
│   ├── lib/
│   │   ├── algolia.ts
│   │   ├── mongodb.ts
│   │   ├── firebase.ts
│   │   ├── telegram.ts
│   │   └── cache.ts
│   ├── data/                   # 静态数据
│   │   ├── projects.json
│   │   ├── tools.json
│   │   └── categories.json
│   └── styles/
│       └── globals.css
├── public/
│   ├── projects/               # 项目截图
│   └── icons/                  # 工具图标
├── functions/                  # Firebase Functions
│   ├── webhooks.ts
│   └── tasks.ts
├── scripts/
│   ├── deploy.sh
│   ├── init-db.js
│   └── seed-data.js
├── package.json
├── next.config.js
├── firebase.json
├── tailwind.config.js
└── tsconfig.json
```

### 核心模块

#### 1. 项目导航首页 (`/`)

```typescript
// src/app/page.tsx
import { Suspense } from 'react';
import ProjectGrid from '@/components/ProjectGrid';
import SearchBar from '@/components/SearchBar';
import ToolsPanel from '@/components/ToolsPanel';

export default function Home() {
  return (
    <main className="min-h-screen">
      {/* 搜索栏 */}
      <section className="bg-gradient-to-r from-blue-600 to-purple-600 py-12">
        <div className="container mx-auto">
          <h1 className="text-4xl font-bold text-white mb-6">极客母艦</h1>
          <p className="text-lg text-blue-100 mb-8">
            汇聚所有技术项目、工具和知识的中央枢纽
          </p>
          <SearchBar />
        </div>
      </section>

      {/* 特色项目 */}
      <section className="py-12 px-4">
        <h2 className="text-3xl font-bold mb-8">特色项目</h2>
        <Suspense fallback={<div>加载中...</div>}>
          <ProjectGrid featured={true} limit={6} />
        </Suspense>
      </section>

      {/* AI 工具展示 */}
      <section className="py-12 px-4 bg-gray-50">
        <h2 className="text-3xl font-bold mb-8">AI 工具</h2>
        <ToolsPanel />
      </section>

      {/* 最新更新 */}
      <section className="py-12 px-4">
        <h2 className="text-3xl font-bold mb-8">最新更新</h2>
        <RecentUpdates />
      </section>
    </main>
  );
}
```

#### 2. 搜索功能集成

```typescript
// src/lib/algolia.ts
import algoliasearch from 'algoliasearch';

const algoliaClient = algoliasearch(
  process.env.NEXT_PUBLIC_ALGOLIA_APP_ID,
  process.env.NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY
);

const projectsIndex = algoliaClient.initIndex('projects');
const docsIndex = algoliaClient.initIndex('documents');

export async function searchProjects(query: string) {
  const results = await projectsIndex.search(query, {
    hitsPerPage: 20,
    filters: 'status:published',
  });
  return results.hits;
}

export async function searchDocs(query: string) {
  const results = await docsIndex.search(query, {
    hitsPerPage: 10,
  });
  return results.hits;
}
```

#### 3. Telegram 机器人集成

```typescript
// src/app/api/webhook/telegram.ts
import { Telegraf } from 'telegraf';
import { searchProjects } from '@/lib/algolia';

const bot = new Telegraf(process.env.TELEGRAM_BOT_TOKEN);

bot.command('projects', async (ctx) => {
  const { query } = ctx.message.text.split(' ');
  if (!query) {
    return ctx.reply('请使用 /projects <关键词>');
  }

  const results = await searchProjects(query);
  const message = results
    .slice(0, 5)
    .map((p: any) => `📌 <b>${p.name}</b>\n${p.description}`)
    .join('\n\n');

  ctx.reply(message || '未找到相关项目', { parse_mode: 'HTML' });
});

export default bot;
```

### 开发流程

```bash
# 1. 安装依赖
cd /mnt/sms/02_核心产品/oece-tech
npm install

# 2. 配置环境变量 (from Doppler)
doppler run --project oece-tech-prod --config dev -- npm run dev

# 3. 访问本地应用
# http://localhost:3000

# 4. 构建与部署
doppler run --project oece-tech-prod --config prod -- npm run build
./scripts/deploy.sh prod
```

### 关键 API 端点

```
GET /api/projects                 # 获取所有项目
GET /api/projects/:id            # 获取项目详情
POST /api/search                 # 全文搜索
GET /api/tools                   # 获取 AI 工具
POST /api/webhook/telegram       # Telegram 回调
GET /api/docs/:slug              # 获取文档
```

---

## 🚀 项目 2: deepweay.me

### 项目结构
```
deepweay-me/
├── apps/
│   ├── web/                      # Web 应用
│   │   ├── src/
│   │   │   ├── app/
│   │   │   │   ├── layout.tsx
│   │   │   │   ├── page.tsx      # 首页
│   │   │   │   ├── chat/
│   │   │   │   │   ├── page.tsx  # 聊天列表
│   │   │   │   │   └── [id]/
│   │   │   │   │       └── page.tsx
│   │   │   │   ├── knowledge/
│   │   │   │   │   └── page.tsx
│   │   │   │   ├── profile/
│   │   │   │   │   └── page.tsx
│   │   │   │   ├── api/
│   │   │   │   │   ├── chat/
│   │   │   │   │   ├── auth/
│   │   │   │   │   ├── knowledge/
│   │   │   │   │   └── webhook/
│   │   │   │   │       └── telegram.ts
│   │   │   ├── components/
│   │   │   │   ├── ChatPanel.tsx
│   │   │   │   ├── MessageBubble.tsx
│   │   │   │   ├── KnowledgeCard.tsx
│   │   │   │   ├── ModelSelector.tsx
│   │   │   │   └── EbookUploader.tsx
│   │   │   ├── lib/
│   │   │   │   ├── gemini.ts
│   │   │   │   ├── firebase.ts
│   │   │   │   ├── telegram.ts
│   │   │   │   ├── storage.ts
│   │   │   │   └── hooks/
│   │   │   │       ├── useChat.ts
│   │   │   │       └── useAuth.ts
│   │   │   └── styles/
│   │
│   └── bot/                      # Telegram Bot
│       ├── src/
│       │   ├── handlers/
│       │   │   ├── chat.ts
│       │   │   ├── commands.ts
│       │   │   └── tools.ts
│       │   ├── services/
│       │   │   ├── GeminiService.ts
│       │   │   └── FirebaseService.ts
│       │   └── bot.ts
│       └── package.json
│
├── packages/
│   ├── types/
│   │   ├── chat.ts
│   │   ├── knowledge.ts
│   │   └── auth.ts
│   └── utils/
│       ├── markdown.ts
│       └── formatting.ts
│
├── data/
│   ├── novels/                   # MD 项目导入
│   ├── knowledge/                # 知识库导入
│   └── seed/
│       ├── novels.json
│       └── knowledge.json
│
├── scripts/
│   ├── deploy.sh
│   ├── seed-data.js
│   └── convert-ebook.sh
│
├── docker-compose.yml
├── package.json
└── pnpm-workspace.yaml
```

### 核心模块

#### 1. 聊天界面 (`/chat/[id]`)

```typescript
// apps/web/src/app/chat/[id]/page.tsx
'use client';

import { useChat } from '@/lib/hooks/useChat';
import ChatPanel from '@/components/ChatPanel';
import MessageBubble from '@/components/MessageBubble';
import ModelSelector from '@/components/ModelSelector';

export default function ChatPage({ params }: { params: { id: string } }) {
  const { chat, messages, sendMessage, loading } = useChat(params.id);

  if (!chat) return <div>加载中...</div>;

  return (
    <div className="h-screen flex flex-col bg-white">
      {/* 头部 */}
      <div className="border-b p-4 flex justify-between items-center">
        <h1 className="text-2xl font-bold">{chat.title}</h1>
        <ModelSelector chat={chat} />
      </div>

      {/* 消息列表 */}
      <div className="flex-1 overflow-auto p-4 space-y-4">
        {messages.map((msg) => (
          <MessageBubble key={msg.id} message={msg} />
        ))}
      </div>

      {/* 输入框 */}
      <ChatPanel onSend={sendMessage} loading={loading} />
    </div>
  );
}
```

#### 2. Gemini AI 集成

```typescript
// apps/web/src/lib/gemini.ts
import { GoogleGenerativeAI } from "@google/generative-ai";

const genAI = new GoogleGenerativeAI(process.env.NEXT_PUBLIC_GEMINI_API_KEY);

export async function createChat(model: string = 'gemini-pro') {
  return genAI.getGenerativeModel({ model });
}

export async function sendMessage(
  chatId: string,
  message: string,
  context: string[] = []
) {
  const model = genAI.getGenerativeModel({ model: 'gemini-pro' });

  const chat = model.startChat({
    history: context.map((c) => ({
      role: 'user',
      parts: c,
    })),
  });

  const result = await chat.sendMessageStream(message);

  return result;
}

export async function summarizeText(text: string): Promise<string> {
  const model = genAI.getGenerativeModel({ model: 'gemini-pro' });

  const result = await model.generateContent(
    `请用中文总结以下文本，用3-5句话表述核心观点:\n\n${text}`
  );

  return result.response.text();
}
```

#### 3. Firebase 聊天存储

```typescript
// apps/web/src/lib/firebase.ts
import { getFirestore, collection, addDoc, query, where, orderBy, getDocs } from 'firebase/firestore';
import { getAuth } from 'firebase/auth';

const db = getFirestore();
const auth = getAuth();

export async function createChat(title: string) {
  const user = auth.currentUser;
  if (!user) throw new Error('用户未登录');

  const chatRef = await addDoc(collection(db, 'chats'), {
    userId: user.uid,
    title,
    createdAt: new Date(),
    model: 'gemini-pro',
  });

  return chatRef.id;
}

export async function saveMessage(
  chatId: string,
  role: 'user' | 'assistant',
  content: string,
  tokens: number
) {
  const user = auth.currentUser;
  if (!user) throw new Error('用户未登录');

  await addDoc(collection(db, `chats/${chatId}/messages`), {
    role,
    content,
    timestamp: new Date(),
    tokens,
  });
}

export async function getMessages(chatId: string) {
  const q = query(
    collection(db, `chats/${chatId}/messages`),
    orderBy('timestamp', 'asc')
  );

  const snapshot = await getDocs(q);
  return snapshot.docs.map((doc) => ({
    id: doc.id,
    ...doc.data(),
  }));
}
```

#### 4. 知识库系统

```typescript
// apps/web/src/lib/knowledge.ts
import { getFirestore, collection, query, where, getDocs } from 'firebase/firestore';

const db = getFirestore();

export async function searchKnowledge(searchTerm: string) {
  const q = query(
    collection(db, 'knowledge'),
    where('tags', 'array-contains', searchTerm)
  );

  const snapshot = await getDocs(q);
  return snapshot.docs.map((doc) => ({
    id: doc.id,
    ...doc.data(),
  }));
}

export async function getRelatedKnowledge(chatId: string) {
  // 基于聊天内容推荐相关知识
  const q = query(
    collection(db, 'knowledge'),
    where('relatedChats', 'array-contains', chatId)
  );

  const snapshot = await getDocs(q);
  return snapshot.docs.map((doc) => ({
    id: doc.id,
    ...doc.data(),
  }));
}
```

### 开发流程

```bash
# 1. 安装依赖
cd /mnt/sms/02_核心产品/deepweay-me
npm install

# 2. 配置环境变量
doppler run --project deepweay-prod --config dev -- npm run dev

# 3. 启动 Web 应用
npm run dev:web

# 4. 启动 Telegram Bot
npm run dev:bot

# 5. 访问应用
# http://localhost:3000

# 6. 部署
./scripts/deploy.sh prod
```

### 关键 API 端点

```
POST /api/chat                   # 创建新聊天
POST /api/chat/:id/message       # 发送消息
GET /api/chat/:id                # 获取聊天详情
GET /api/chat                    # 获取用户聊天列表
GET /api/knowledge               # 搜索知识库
POST /api/webhook/telegram       # Telegram 回调
POST /api/tools/ebook/convert    # 电子书转换
```

---

## 📊 两个网站的关键区别

### oece.tech (导航+展示)
- **重点**: 用户体验和导航体验
- **优化**: SEO、加载速度、搜索体验
- **成本驱动**: Algolia 搜索费用
- **扩展**: 添加更多项目、工具、知识

### deepweay.me (AI+交互)
- **重点**: AI 能力和实时交互
- **优化**: AI 模型能力、响应速度、用户参与度
- **成本驱动**: Gemini API 调用费用
- **扩展**: 更多 AI 功能、内容生成、自动化

---

## 🔧 共同的开发工具

### 共享组件库 (packages/)

```typescript
// packages/ui/Button.tsx
import { ReactNode } from 'react';

interface ButtonProps {
  children: ReactNode;
  onClick?: () => void;
  variant?: 'primary' | 'secondary';
  loading?: boolean;
}

export function Button({ children, variant = 'primary', loading, ...props }: ButtonProps) {
  return (
    <button
      className={`px-4 py-2 rounded font-medium transition ${
        variant === 'primary'
          ? 'bg-blue-600 text-white hover:bg-blue-700'
          : 'bg-gray-200 text-gray-800 hover:bg-gray-300'
      } ${loading ? 'opacity-50 cursor-not-allowed' : ''}`}
      disabled={loading}
      {...props}
    >
      {loading ? '加载中...' : children}
    </button>
  );
}
```

### 共享类型定义 (packages/types/)

```typescript
// packages/types/index.ts
export interface User {
  id: string;
  email: string;
  name: string;
  avatar?: string;
  createdAt: Date;
}

export interface Project {
  id: string;
  name: string;
  description: string;
  url: string;
  tags: string[];
}

export interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
}
```

---

## 📈 性能优化

### 缓存策略

```typescript
// lib/cache.ts
import { Upstash } from "@upstash/redis";

const redis = new Upstash.Redis({
  url: process.env.UPSTASH_REDIS_URL,
  token: process.env.UPSTASH_REDIS_TOKEN,
});

// Cache with TTL
export async function cacheGet<T>(key: string): Promise<T | null> {
  const cached = await redis.get(key);
  return cached as T | null;
}

export async function cacheSet<T>(key: string, value: T, ttl: number = 3600) {
  await redis.setex(key, ttl, JSON.stringify(value));
}
```

### 图片优化

```typescript
// 使用 Next.js Image 组件
import Image from 'next/image';

export function ProjectCard({ project }) {
  return (
    <div>
      <Image
        src={project.image}
        alt={project.name}
        width={300}
        height={200}
        priority
        quality={80}
      />
    </div>
  );
}
```

---

## 🚀 部署检查清单

### Pre-Deployment
- [ ] 所有环境变量已配置
- [ ] 数据库初始化完成
- [ ] 单元测试通过
- [ ] 没有 TypeScript 错误
- [ ] 没有未提交的更改

### Deployment
- [ ] 构建成功
- [ ] Firebase/GCP 部署成功
- [ ] 域名 DNS 指向正确
- [ ] SSL 证书有效
- [ ] Sentry 监控已启用

### Post-Deployment
- [ ] 访问网站正常
- [ ] API 调用成功
- [ ] 数据库连接正常
- [ ] 监控告警配置完成

---

**最后更新**: 2026-01-15 15:00:00
