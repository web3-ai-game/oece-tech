# 🔄 緩存清除指南 - oece.tech 顯示舊版本

## 問題原因

Firebase Hosting 有 CDN 緩存：
- **Cache-Control**: max-age=3600（1 小時）
- **舊版本**: 還在 CDN 緩存中
- **新版本**: 正在部署

---

## ✅ 解決方案

### 方法 1：強制刷新瀏覽器（最快）

#### Chrome/Edge
```
Cmd + Shift + R (Mac)
Ctrl + Shift + R (Windows)
```

#### Safari
```
Cmd + Option + R
```

#### 或清除瀏覽器緩存
```
Chrome: 設置 → 隱私和安全 → 清除瀏覽數據 → 緩存的圖片和文件
```

---

### 方法 2：使用無痕模式

```
Cmd + Shift + N (Chrome)
Cmd + Shift + P (Safari)
```

無痕模式不會使用緩存，直接訪問最新版本。

---

### 方法 3：添加查詢參數

```
https://oece-tech-9aa8d.web.app?v=2
https://oece.tech?nocache=1
```

---

### 方法 4：等待 CDN 緩存過期

- **時間**: 最多 1 小時
- **自動**: CDN 會自動更新

---

### 方法 5：Firebase CLI 清除緩存（不推薦）

```bash
# Firebase Hosting 沒有直接清除緩存的命令
# 只能等待自然過期或重新部署
```

---

## 🔧 修改 firebase.json（減少緩存時間）

```json
{
  "hosting": {
    "source": ".",
    "headers": [
      {
        "source": "**",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=300"
          }
        ]
      },
      {
        "source": "**/*.@(js|css)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=31536000, immutable"
          }
        ]
      }
    ]
  }
}
```

這會將 HTML 緩存時間從 1 小時改為 5 分鐘。

---

## 📊 檢查部署狀態

### GitHub Actions
```bash
gh run list --repo web3-ai-game/oece-tech --limit 1
```

### 查看最新部署
```
https://github.com/web3-ai-game/oece-tech/actions
```

---

## 🎯 當前狀態

- **最新 Commit**: `50de50c` - Implement Firebase Auth
- **部署狀態**: 🔄 進行中
- **預計完成**: 2-3 分鐘
- **CDN 緩存**: 最多 1 小時

---

## 💡 建議

### 立即查看最新版本
1. **無痕模式** 訪問 https://oece-tech-9aa8d.web.app
2. **強制刷新** Cmd+Shift+R
3. **等待部署完成**（2-3 分鐘）

### 長期解決
1. 修改 `firebase.json` 減少緩存時間
2. 使用版本號查詢參數
3. 配置 CDN 清除策略

---

**問題**: 舊版本緩存  
**原因**: CDN 1 小時緩存  
**解決**: 強制刷新或無痕模式
