以下是根據您提供的文檔輸出：

### 1. 極精簡的一段摘要

本手冊旨在指導如何在零成本下，最大化利用 Gemini AI 免費層資源。核心策略是精準調用可靠模型，避免「假無限」陷阱，並透過分階段部署、用戶分級及嚴格監控，確保資源永續利用並逐步實現商業價值。

### 2. 條列式關鍵要點

*   **核心目標**：在零成本前提下，從 Gemini AI 免費層榨取最大價值，初期目標為支援 50 用戶。
*   **警惕「假無限」**：Live API 號稱「無限制」實為心理戰陷阱，實際存在嚴格的 TPM、RPD 及動態限流，不適用於生產環境。
*   **模型分級使用**：區分可靠（Flash-Lite, Flash）、謹慎（Pro）及不可靠（Live API 系列、實驗性模型）的 Gemini 模型，並根據其性能與可靠度分配戰術用途。
*   **分階段部署策略**：依據模型特性與用量限制，規劃 Alpha (Flash-Lite)、Bravo (Flash) 和 Charlie (Pro) 三個階段的應用部署，確保資源合理利用。
*   **用戶分級與變現**：建立 Guest、Free、Pro 三層用戶體系，透過限制與增值服務引導用戶轉化，實現月經常性收入。
*   **嚴格監控與預警**：實施每日使用率監控，並設定綠、黃、紅三級預警機制，以便及時優化調用或採取限流/升級措施。
*   **技術架構支持**：設計 API Key 池管理、智能路由器、速率限制器及降級策略等技術組件，以高效管理和調度免費 API 資源。

### 3. 可執行建議

1.  **優先部署核心免費功能，嚴格控制模型選用**：按照「階段 ALPHA」和「階段 BRAVO」的規劃，優先將 Flash-Lite 和 Flash 模型應用於翻譯、簡單對話、TG Bot 等核心免費功能，並嚴格禁止在生產環境中使用任何被標記為「不可靠」或「實驗性」的模型，以確保服務穩定性。
2.  **立即構建並啟用 API Key 池與監控系統**：在開發初期就將 `pool.go`、`router.go`、`limiter.go` 等關鍵組件納入核心架構，並配置 `keys.yaml` 中的多個免費 API Key。同時，啟用「監控系統」中的每日情報收集清單，確保能實時追蹤各模型使用率，並在達到預警閾值時立即採取對應行動。
3.  **基於用戶數據迭代優化用戶分級與付費轉化**：在「用戶等級戰術」實施後，持續收集 Guest、Free、Pro 用戶的行為