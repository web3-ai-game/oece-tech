以下是針對您提供的技術文檔的蒸餾與擴寫：

### 1. 極精簡的一段摘要 (不超過 80 字)

這份文檔提出一套 Gemini API Key 調用策略，以「速率優先」為核心。它將付費 Key 用於深度思考、精確代碼生成等高價值任務，追求極速爆發；同時利用免費 Key 進行高頻、低成本的兜底噴射，如群聊互動。智能路由確保根據任務類型和成本考量，動態選擇最適合的模型，並實施速率限制。

### 2. 條列式關鍵要點 (3-7 條)

*   **核心原則：** 優先使用付費 Key 處理高價值、需深度思考的任務，免費 Key 則作為高頻、低成本任務的兜底。
*   **模型分層：** 將 Gemini 模型分為「付費層」與「免費層」，並詳細列出各模型的 RPM、TPM、成本與推薦用途。
*   **智能路由：** 建立 `SmartKeyRouter` 實現根據任務類型（如深度思考、代碼生成、群聊互動）動態選擇最合適的 Key。
*   **速率限制：** 內建令牌桶機制，確保 Key 的調用不會超出其速率限制（RPM），避免因超限導致服務中斷。
*   **特定用途推薦：** 推薦 Gemini 3 Pro Preview 用於精確代碼與數據清洗，Gemini 2.5 Flash-Lite 用於高頻免費群聊互動。
*   **環境變量管理：** 建議將所有 API Keys 配置為環境變量，以提高安全性和部署靈活性。

### 3. 可執行建議

1.  **立即實施智能 Key 路由邏輯：** 根據文檔提供的 `SmartKeyRouter` 類，將其集成到應用中，確保不同任務能自動匹配最優的付費或免費 Key，實現速率與成本的最佳平衡。
2.  **建立 Key 使用監控與預警機制：** 除了內建的速率限制，應進一步監控各 Key 的實際調用量、成功率及成本，並設定預警，以便及時調整策略或補充 Key 資源，避免服務中斷或成本超支。
3.  **定期評估與更新模型策略：** 隨著 AI 模型技術的發展和成本變化，應定期審查「模型速率對照表」和「調用優先級策略」，引入新的高效模型或調整現有模型的分配，以保持策略的先進性和有效性。