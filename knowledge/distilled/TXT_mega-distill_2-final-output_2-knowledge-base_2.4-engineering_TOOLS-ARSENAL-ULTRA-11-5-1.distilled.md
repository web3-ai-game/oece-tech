---
source: TXT_mega-distill_2-final-output_2-knowledge-base_2.4-engineering_TOOLS-ARSENAL-ULTRA-11-5-1.md
distilled_at: 2026-02-14T09:17:01.783Z
model: grok-4-1-fast-non-reasoning
---

# 🛠️ 工具武器庫 | TOOL ARSENAL

**類別**: 2-knowledge-base/2.4-engineering  
**來源**: docs/05-資源與工具/TOOLS-ARSENAL-ULTRA.md  
**蒸餾者**: grok-4-0709  
**模式**: B  
**部分**: 11  

---

## 5.1 使用Git的簡單版本控制

### 介紹與脈絡
Git 是一款免費、開源的分散式版本控制系統，由 Linus Torvalds 於 2005 年開發，用於 Linux 内核開發。它允許開發者追蹤程式碼變更、協作管理專案，並輕鬆回溯歷史版本。在軟體工程中，Git 是業界標準工具，尤其與 GitHub、GitLab 等平台整合，能實現遠端儲存與團隊協作。

本節聚焦**最簡易的 Git 工作流程**：從本地初始化倉庫，到推送至 GitHub。這適合初學者快速上手，建立第一個版本控制專案。假設你已在本地安裝 Git（透過 `git --version` 驗證），並擁有 GitHub 帳號。

**為何使用 Git？**
- **版本追蹤**：記錄每次變更，避免遺失程式碼。
- **分支管理**：安全實驗新功能（進階主題）。
- **協作**：多人同時編輯，合併衝突自動解決。
- **備份**：遠端推送至 GitHub，即免費雲端備份。

---

### 核心 Git 命令序列
以下是完整步驟，按順序執行。假設你在專案資料夾中（例如 `my-project`），並已建立一些檔案（如 `README.md`、`index.html`）。

```bash
# 1. 初始化 Git 倉庫
# 這會在當前目錄建立 .git 隱藏資料夾，啟動版本控制
git init

# 2. 添加檔案至暫存區
# git add . 將當前目錄所有變更檔案加入（包含新檔案與修改）
git add .

# 3. 提交變更
# -m 參數提供提交訊息，記錄本次變更原因。保持簡潔、有意義
git commit -m "Initial commit"

# 4. 添加遠端 GitHub 倉庫
# 先在 GitHub 建立空倉庫（repo），複製其 HTTPS URL 替換下方連結
git remote add origin https://github.com/user/repo.git

# 5. 推送至 GitHub（並設定上游分支）
# -u 參數設定 origin/main 為預設上游，之後只需 git push
git push -u origin main
```

**執行後驗證**：
- 本地：`git status`（顯示乾淨狀態）。
- GitHub：刷新頁面，確認檔案已上傳。

---

### 常見問題與提示
| 情境 | 解決方案 |
|------|----------|
| **分支名稱錯誤**（預設 master 而非 main） | `git branch -M main` 在 commit 前執行。 |
| **推送失敗（認證）** | 使用 GitHub Personal Access Token（非密碼），或設定 SSH 金鑰。 |
| **已存在遠端** | `git remote remove origin` 後重新添加。 |
| **忽略檔案** | 建立 `.gitignore` 檔案，加入如 `node_modules/`、`*.log`。 |
| **查看歷史** | `git log --oneline`（簡潔歷史）。 |

**進階提示**：
- **日常循環**：編輯檔案 → `git add .` → `git commit -m "描述"` → `git push`。
- **拉取更新**：`git pull origin main`（從遠端同步）。
- **撤銷**：`git checkout -- filename`（還原檔案）；`git reset HEAD~1`（撤銷上次 commit）。

---

### 範例輸出
執行後，終端典型回應：
```
$ git init
Initialized empty Git repository in /path/to/my-project/.git/

$ git add .
$ git commit -m "Initial commit"
[main (root-commit) abc1234] Initial commit
 2 files changed, 10 insertions(+)

$ git push -u origin main
Enumerating objects: 5, done.
...
To https://github.com/user/repo.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

此流程只需 1 分鐘，即可將本地專案轉為 GitHub 倉庫。繼續探索 Git 以掌握完整力量！ 

**參考資源**：
- [Git 官方文件](https://git-scm.com/docs)
- [GitHub 快速入門](https://docs.github.com/en/get-started)